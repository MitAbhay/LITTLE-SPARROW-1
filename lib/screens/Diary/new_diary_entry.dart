import 'package:flutter/services.dart';
import 'package:flutter/material.dart';

class NewDiaryEntry extends StatelessWidget {
  const NewDiaryEntry({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
