import 'package:flutter/material.dart';
import 'package:flutter/services.dart';

class OldDiaryEntry extends StatelessWidget {
  const OldDiaryEntry({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
